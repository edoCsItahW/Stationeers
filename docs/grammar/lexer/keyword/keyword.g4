grammar IC10;

import nullary;
import unary;
import binary;
import ternary;
import quaternary;
import quinary;
import senary;

// 别名指令
KeywordAlias : 'alias';

// 定义指令
KeywordDefine : 'define';

// 宏
KeywordHash : 'HASH';
KeywordStr  : 'STR';
